package com.pzhu.mall.config;

import org.springframework.context.annotation.Configuration;

/**
 * MyBatis-Plus auto-fill handler - NOT ACTIVE.
 * Database DEFAULT CURRENT_TIMESTAMP handles create_time/update_time.
 * Enable this if entity fields need Java-side filling.
 */
@Configuration
public class MetaObjectHandlerConfig {
    // Empty - database DEFAULT constraints handle timestamps
}
