spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useServerPrepStmts=true&cachePrepStmts=true
  redis:
    host: ${REDIS_HOST:localhost}
    password: ${REDIS_PASSWORD:}

jwt:
  secret: ${JWT_SECRET}

logging:
  level:
    com.pzhu.mall: WARN
