<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pzhu.mall.modules.product.mapper.SkuMapper">

  <update id="deductStock">
    UPDATE sku
    SET stock = stock - #{quantity},
        update_time = NOW()
    WHERE id = #{skuId}
      AND stock >= #{quantity}
      AND is_deleted = 0
  </update>

  <!-- O-01 修复：退款审核通过后归还 SKU 库存 -->
  <update id="restoreStock">
    UPDATE sku
    SET stock = stock + #{quantity},
        update_time = NOW()
    WHERE id = #{skuId}
      AND is_deleted = 0
  </update>

</mapper>
