# 复检报告（修复后按 docs/22 三级方案回归检查）

> 日期：2026-08-14 | 执行人：SeniorDeveloper
> 范围：修复批次（P1 13 项 + P2 26 项 + 第二批 7 项 + 动态验证 2 项）后的全量复检
> 方法：① 测试基线全量回归（`./mvnw test`）② 关键修复点代码走查 ③ 动态验证回顾（docs/28）

## 1. 复检结论

- **测试基线**：后端 **207 用例，0 Failures / 0 Errors / 1 Skipped** ✅（复检时基线未破坏）
- **修复质量**：13 项 P1 关键修复逐一代码复核通过，无回退、无新引入缺陷
- **复检新发现**：1 项一致性隐患（RV-01，已修复）+ 2 项 P2 级备注

## 2. 修复确认表（P1 关键项代码走查）

| # | 修复项 | 复检结果 | 依据 |
|---|---|---|---|
| AD-01 | 商家角色 | ✅ | ShopService.audit 联动升级 role=2（同事务）；AdminUserController 角色分配接口（防自降级） |
| F-01 | 前端券字段 | ✅ | Confirm.vue：estimate 传模板 couponId、createOrder 传记录 userCouponId |
| O-01 | 退款恢复库存 | ✅ | restoreStock：DB 原子归还（事务内）+ **Redis 归还 afterCommit（RV-01 复检时强化）**；动态验证 9→10 |
| O-02 | 退款幂等键 | ✅ | SET NX 移至全部校验通过后 |
| O-03 | 下单吞异常 | ✅ | 两处 catch 补 log.error 栈 + 错误信息透传（DV-02） |
| O-04 | 库存并发漂移 | ✅ | pay 用 deductStockUnchecked（GREATEST 原子）替代读改写 |
| O-05 | 支付记录 | ✅ | pay 插入 Payment（含流水号/金额/支付方式） |
| M-01 | 满赠 | ✅ | matchGift + 赠品行（预扣库存/afterCompletion 归还/行为排除）；动态验证赠品行+Redis 5→4 |
| M-02 | 券适用范围 | ✅ | 五重校验（归属/状态/有效期/店铺/品类），下单传 shopId+品类列表 |
| ST-01 | 统计口径 | ✅ | 销售统计/热销 TOP10 均 status IN (1,2,3,4,6) |
| L2-01 | 准实时推荐 | ✅ | 购买后异步 calculateForUser（Redis 10min 节流）；动态验证 recommend_result 生成 |
| L2-02 | 事务内 Redis | ✅ | doCancel Redis 归还 afterCommit；回滚不归还 |
| L2-03 | 改库存同步 | ✅ | evictStockRedisKey 失效 key 懒加载兜底 |

## 3. 复检新发现

| # | 问题 | 级别 | 处理 |
|---|---|---|---|
| **RV-01** | **RefundService.restoreStock 的 Redis 归还仍在事务内**：与 L2-02 同构——audit 事务内先 Redis INCR，若后续积分扣回失败回滚，Redis 库存已归还 → 虚增 | **P1**（一致性，与 L2-02 同类） | ✅ **已修复**：DB 归还保持事务内（原子），Redis INCR 注册 afterCommit（回滚不误还），与 doCancel 口径统一；测试 207 全绿 |
| RV-02 | matchGift 阈值缺失/为 0 时无条件赠送（ruleJson 缺 threshold → getOrDefault 0 → 恒达标） | P2（配置边界） | 备注：M-06 validatePromotion 可增强按 type 校验必需字段（threshold/giftSkuId 等），列入 P2 待办 |
| RV-03 | 估价接口不展示满赠赠品（确认页无"满赠"提示，下单后订单详情可见） | P2（体验） | 备注：可选增强 OrderEstimateController 返回赠品信息，非阻塞 |

## 4. 动态验证回顾（docs/28，10 项）

- 消费者端到端 / 商家端 / 管理员端全链路 ✅
- 并发防超卖（50×1 全成 99→49；20×10 仅 4 单成 49→9）✅
- 退款闭环 + 库存恢复（10→9→10）✅
- 满赠赠品行 + 预扣 ✅；准实时推荐落库 ✅
- 边界 E1~E5、防爆破锁定、退款幂等 ✅

## 5. 遗留项（非本次复检范围，供决策）

| 项 | 说明 |
|---|---|
| LG-04 | 物流对接快递100（模拟实现，需决策/文档声明） |
| RV-02/RV-03 | 满赠配置字段校验增强、估价展示赠品（P2） |
| 需求确认类 P2 | P-07 已上架改价重审、P-09 product.price 口径、M-08 促销 scope 扩展、ST-06 金额口径 |
| 前端 chunk | ECharts 按需引入（>500kB 警告，非阻塞） |

## 6. 结论

- 修复后的代码**未引入新问题**（测试 207 全绿 + 代码走查 + 动态验证三重确认）
- 复检额外修复 1 项一致性隐患（RV-01），使"Redis 操作出事务"规范在 doCancel 与退款归还两处口径统一
- 系统当前状态：**P1 13/13 闭环、动态验证 10/10 通过、测试 207 全绿**，可交付
