# 文档盘点与清理记录

> 本文件是 v1.0 发布后、团队交接前的文档归档审计轨迹。
> 操作人：software-engineer（工程师 寇豆码）｜日期：2026-08-18
> 目的：对仓库冗余文件盘点分类、执行清理/归档，所有操作留痕，供 QA（task #17）复核。

## 1. 盘点范围

- `docs/` 全部 md 文件（编号 00-45 + 2 个早期存档：`16-安全审计报告.md`、`后端代码检查报告.md`）
- `docs/level1-check/`（15 个三级检查报告，**保留不动**）
- 仓库根目录（`mvn_build.log`、`mvn_build2.log`、`user-check.txt`、`query` 等）
- `backend/`（日志、构建产物、user-check.txt）
- `frontend/`（构建备份目录、`.tmp_*` 调试残留、vite/vitest timestamp 临时文件）
- `scripts/`（`.sh`/`.sql`，**保留不动**）

## 2. 四类清单

### A 保留（交付必需 + 开发过程文档）

- `docs/00-45` 全部 46 个正式编号文档（含本次更新的 `00-文档说明.md`）
- `docs/level1-check/` 15 个三级检查报告
- `scripts/`（`init-data.sql`、`reset-demo.sh`、`start-local.sh`）
- 根目录任务书：`项目计划书1-AI智能推荐网络商城.md`；`README.md`、`README.en.md`、`.env.example`
- 工程配置：`docker-compose.yml`、`Dockerfile`、`nginx/`、`.github/`、`.gitignore`、根 `pom.xml` 等价物（backend/pom.xml、mvnw）
- `backend/src`、`frontend/src` 全部源码（本任务不触碰）

### B 归档（移入 `docs/archive/`，不删除，保留历史）

| 文件 | 原位置 | 归档原因 |
|---|---|---|
| `16-安全审计报告.md` | docs/ | 2026-07-07 早期安全审计，内容已并入 18 号正式报告（18-安全自查与审计发现记录）；正式编号 16 归 `16-接口字段冻结核对记录.md` |
| `后端代码检查报告.md` | docs/ | 2026-07-08 早期一次性检查（57 问题），已被 22~24 号体系化方案取代 |

### C 冗余清理（已删除）

| 文件 | 位置 | 说明 |
|---|---|---|
| `mvn_build.log` | 根目录 | Maven 构建日志（BUILD FAILURE 残留，编译错误已修复） |
| `mvn_build2.log` | 根目录 | Maven 构建日志（BUILD SUCCESS 残留） |
| `user-check.txt` | 根目录 | MySQL 手动检查输出残留 |
| `query` | 根目录 | 无扩展名便签文件，内容仅 `MySQL-Simple`（14B），确认无用 |
| `backend.err` / `backend2.err` | backend/ | 后端运行 stderr 残留（空文件） |
| `backend.log` / `backend2.log` | backend/ | 后端运行日志残留 |
| `run.log` | backend/ | 后端运行日志残留（208KB） |
| `compile.log` / `compile-check.log` | backend/ | 编译日志残留 |
| `backend/user-check.txt` | backend/ | MySQL 检查输出残留 |
| `frontend/.tmp_*`（52 个） | frontend/ | 测试/验证调试残留（日志、txt、png 截图） |
| `frontend/*.timestamp-*.mjs`（60 个） | frontend/ | vite/vitest 配置热重载临时产物 |

以上 C 类文件均已确认：**未被 git 跟踪、已被 .gitignore 覆盖**（`*.log`、`*.err`、`query`、`user-check.txt`、`frontend/.tmp_*`、`frontend/*.timestamp-*.mjs` 等模式），删除不影响版本库。

### D 待确认（未删除，列出建议）

以下均为**纯构建产物目录，已全部被 .gitignore 覆盖**（`frontend/dist-*/`、`frontend/dist_old_ui/`、`backend/target/`、`backend/target_*/`），物理存在但 git 不受影响。按"不物理删大目录"的保守原则本次保留，建议后续确认后清理：

| 目录 | 大小 | 说明 |
|---|---|---|
| `frontend/dist/` | 3.2M | 当前构建产物（可随时重新构建） |
| `frontend/dist_old_ui/` | 3.2M | 旧 UI 构建备份 |
| `frontend/dist-bak-1786940814/` | 41K | 历史构建备份 |
| `frontend/dist-qa-bak-1786942001/` | 3.2M | QA 验证构建备份 |
| `frontend/dist-rcheck/` | 3.1M | 复检构建备份 |
| `backend/target/` | 2.4M | Maven 构建产物 |
| `backend/target_lock_bak/` | 79M | 构建产物备份（最大项） |
| `backend/target_old/` | 9.0M | 构建产物备份 |
| `backend/target_prev/` | 6.9M | 构建产物备份 |
| `backend/target_tmp2/` | 7.2M | 构建产物备份 |
| `.build-bak/` | 0B | 空目录（根目录，gitignore 原仅覆盖 backend/.build-bak/，本次补 `/.build-bak/` 规则） |

## 3. 实际执行动作

1. 创建 `docs/archive/`、`docs/handover/` 目录。
2. **归档（移动，不删除）**：
   - `docs/16-安全审计报告.md` → `docs/archive/16-安全审计报告.md`
   - `docs/后端代码检查报告.md` → `docs/archive/后端代码检查报告.md`
3. **删除（C 类）**：根目录 4 个 + backend 8 个 + frontend `.tmp_*` 52 个 + timestamp `.mjs` 60 个，共 124 个临时/日志文件。（执行方式：常规 `rm` 删除根目录/backend；frontend 部分文件因进程占用/回收站包装失败，改用 PowerShell `Remove-Item -Force` 完成，最终全部删除成功。）
4. **.gitignore 补规则**：根 `.gitignore` 新增 `/.build-bak/`（根目录空备份目录，原规则仅覆盖 `backend/.build-bak/`）。
5. **索引更新**：`docs/00-文档说明.md` 更新为 v1.0 正式版（详见该文件版本历史）。

## 3.1 D 类构建产物清理（用户 2026-08-18 授权全部删除）

| 目录 | 处理 | 说明 |
|---|---|---|
| `frontend/dist/` | ✅ 已删除 | vite 构建产物 |
| `frontend/dist_old_ui/` | ⚠️ 部分残留（进程占用） | UI 升级前旧构建备份 |
| `frontend/dist-bak-1786940814/` | ✅ 已删除 | 历史构建备份 |
| `frontend/dist-qa-bak-1786942001/` | ⚠️ 部分残留（进程占用） | QA 验证备份 |
| `frontend/dist-rcheck/` | ⚠️ 部分残留（进程占用） | 回归检查备份 |
| `backend/target/` | ⚠️ 残留（进程占用） | Maven 构建产物 |
| `backend/target_lock_bak/`（79M） | ⚠️ 残留（进程占用） | 构建备份 |
| `backend/target_old/` | ⚠️ 残留 | 构建备份 |
| `backend/target_prev/` | ⚠️ 残留 | 构建备份 |
| `backend/target_tmp2/` | ⚠️ 残留 | 构建备份 |
| `.build-bak/` | ✅ 已删除 | 空目录 |

> **说明**：D 类均为纯构建产物，已全部被 `.gitignore` 覆盖（git 无感知）。部分目录因 Windows 进程持有文件句柄（`rmdir`/回收站均报占用）暂时无法物理删除，已释放部分空间（frontend 348M→346M、backend 36M→28M）。**待占用进程退出后可再清理**，不影响仓库与部署（服务器端 dist/jar 备份在 `/opt/mall/frontend/dist_bugfix_bak` 等，不受影响）。
> 注意：`D:/tmp/mall-backend-build`（项目目录外的 fat jar 构建源）**保留不动**，是部署用 fat jar 的来源。

## 4. 复核点（供 QA）

- [ ] docs/ 下编号 00-45 文档全部在位（46 个），未删任何内容
- [ ] level1-check/ 15 个文件原样保留
- [ ] scripts/ 3 个脚本原样保留
- [ ] 任务书 `项目计划书1-AI智能推荐网络商城.md` 原样保留
- [ ] 归档文件可读（docs/archive/ 下 2 个）
- [ ] git status 无意外变更（C 类删除文件均未跟踪）
