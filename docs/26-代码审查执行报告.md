# 代码审查执行报告（基于 docs/25 审查方案 · 批次 0~2）

> **日期**：2026-08-13
> **执行人**：SeniorDeveloper
> **依据**：docs/25-代码审查方案.md（批次 0 工具链 → 批次 1 首次全量审查 → 批次 2 缺陷修复）
> **验证基线**：后端 194 单测（0F/0E）、前端 14 单测、ESLint 0 问题、`mvn verify` 全链路通过

---

## 1. 批次 0：工具链落地（全部完成 ✅）

### 1.1 前端工具链（ESLint + Prettier + scripts）

| 交付物 | 状态 | 说明 |
|---|---|---|
| `frontend/eslint.config.js` | ✅ | ESM flat config（适配 `"type":"module"`）；逻辑规则 error/warn + Vue3 recommended（按项目放宽 8 项）；格式交 Prettier |
| `frontend/.prettierrc.json` | ✅ | semi=false / singleQuote / printWidth=120 |
| `frontend/.prettierignore` | ✅ | node_modules/dist/coverage 等 |
| `package.json` scripts | ✅ | `lint` / `lint:fix` / `format`（flat config 无需 --ext） |
| devDependencies | ✅ | eslint@8.57.1、eslint-plugin-vue@9.33.0、prettier@3.9.6（eslint-config-prettier 未装——本配置未引用，无冲突） |

> ⚠️ **环境说明**：本机 npm 沙箱拦截缓存清理（`D:\npm\npm_cache`），依赖安装曾中断；最终通过沙箱外 + 本地缓存目录完成。`package-lock.json` 因沙箱文件保护未能同步新条目，**CI 已改用 `npm install` 现场解析**（runner 无沙箱限制），见 §4。

### 1.2 后端工具链（Checkstyle + SpotBugs + JaCoCo）

| 交付物 | 状态 | 说明 |
|---|---|---|
| `backend/checkstyle.xml` | ✅ | 规则集：命名/导入/格式（error 级）+ Javadoc/MagicNumber 等（warning 级）；已调优剔除 FinalParameters/SeparatorWrap 噪音 |
| `backend/spotbugs-exclude.xml` | ✅ | 排除：测试类、entity/dto/vo/controller 内部类/task/common.result 的 EI_EXPOSE、DTO 未读字段 |
| `pom.xml` 插件 | ✅ | checkstyle 3.2.2（verify 绑定）、spotbugs 4.9.8.5（verify 绑定，failOnError=false）、jacoco 0.8.11（test 绑定） |
| 首次扫描 | ✅ | **Checkstyle 339 warning / 0 error**；**SpotBugs 55 bugs → 修复后 48 → exclude 后 20 可接受项** |

> ⚠️ **版本说明**：spotbugs 4.8.4 在阿里云镜像缺失，改用 4.9.8.5（中央仓库）；`-Dspotbugs.skip=true` 为本机 target 沙箱写保护下的降级验证方式，**CI（ubuntu）无需 skip**。

### 1.3 CI 质量门禁（.github/workflows/ci.yml）

- frontend job：`npm install` → `npm run lint` → `npm test` → `npm run build`
- backend job：`mvn -B verify`（自动执行 checkstyle:check + spotbugs:check + jacoco:report + test）
- 新增覆盖率/检查报告 artifact 上传（jacoco/checkstyle/spotbugs）

---

## 2. 批次 1：首次全量审查（工具扫描 + 人工复核 ✅）

### 2.1 工具扫描结果

| 工具 | 扫描结果 | 结论 |
|---|---|---|
| **ESLint** | 37 warning → 修复/收敛后 **0 problems** | ✅ 通过 |
| **Checkstyle** | 339 warning（0 error） | 格式类（MagicNumber 116 / Indentation 52 / LineLength 50 / UnusedImports 36 等），不阻塞构建 |
| **SpotBugs** | 55 bugs → **exclude 后 20 项可接受** | High 已清零；Medium/Low 为已知可接受模式 |
| **JaCoCo** | 指令 37.1% / 分支 36.1% | 覆盖率基线（未来门禁参考值） |

### 2.2 人工复核发现并修复的高价值问题（P1）

| # | 位置 | 问题 | 修复 | 验证 |
|---|---|---|---|---|
| **CR-01** | `frontend/views/consumer/order/List.vue` | **契约 bug（P1）**：模板用 `order.id`，后端 OrderVO 字段为 `orderId` → "去支付/查看详情"跳转 `/order/pay/undefined` | 4 处 `order.id` → `order.orderId` | 前端测试 14 过 |
| **CR-02** | `frontend/views/consumer/order/Detail.vue` | **契约 bug（P1）**：`order.value.id` 同理，取消/确认/物流查询全失效 | 5 处 `order.value.id` → `order.value.orderId` | 同上 |
| **CR-03** | `BehaviorService.record` | **NPE（Medium）**：behaviorType=null 时 switch 崩溃（SpotBugs NP_NULL） | null 兜底为浏览(1) | 编译+测试通过 |
| **CR-04** | `JwtUtil.init` | **High DM_DEFAULT_ENCODING**：`getBytes()` 依赖平台默认编码 | 显式 `StandardCharsets.UTF_8` | 编译通过 |

### 2.3 人工复核发现并修复的规范问题

| # | 位置 | 问题 | 修复 |
|---|---|---|---|
| CR-05 | RecommendCalculateService ×2 | WMI_WRONG_MAP_ITERATOR（keySet 循环内 get） | 改 entrySet 遍历 |
| CR-06 | MerchantStatisticsService ×1 | 同上 | 改 entrySet 遍历 |
| CR-07 | RefundService/MerchantProductController ×2 | DE_MIGHT_IGNORE（空 catch 无日志） | 补 debug 日志 |
| CR-08 | StockService | UPM_UNCALLED_PRIVATE_METHOD（loadFromDb 死代码） | 删除（Lua 已内置懒加载） |
| CR-09 | RecommendCalculateService ×2 | BX_UNBOXED（Integer 三元装箱强转） | 显式 `.intValue()` |
| CR-10 | 前端 5 处 | no-unused-vars（测试 import/未用参数/常量） | 清理或 `_` 前缀 |

### 2.4 SpotBugs exclude 后剩余 20 项（登记为"已知可接受"）

| 模式 | 数量 | 说明 | 处置 |
|---|---|---|---|
| REC_CATCH_EXCEPTION | 9 | JSON 解析容错 catch（设计行为） | 已知可接受 |
| NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE | 3 | selectById 判空路径（已有 if 判空） | 已知可接受 |
| RCN_REDUNDANT_NULLCHECK | 3 | 冗余判空（编译器已知非空） | 低危，可后续清理 |
| DE_MIGHT_IGNORE / BX / THROWS / URF | 5 | 各类告警级 | 低危 |

> **说明**：EI_EXPOSE_REP/REP2 共 28 项（DTO/VO 集合浅拷贝）已按规则排除——REST 序列化常规模式，防御性拷贝对毕设场景收益低；如需严格可后续在关键 VO 加 `Collections.unmodifiableList`。

---

## 3. 批次 2：缺陷修复与回归（✅）

| 验证项 | 结果 |
|---|---|
| 后端 `mvn verify`（含测试） | ✅ BUILD SUCCESS，**194 用例 0F/0E/1S** |
| 后端 Checkstyle | ✅ 339 warning / 0 error（不阻塞） |
| 后端 JaCoCo | ✅ 指令 37.1% / 分支 36.1%（报告已生成） |
| 前端 ESLint | ✅ **0 problems** |
| 前端 vitest | ✅ **14 用例全过**（含 F-01 契约测试） |
| 后端 SpotBugs | ✅ High 清零；exclude 后 20 可接受项（failOnError=false 不阻塞） |

---

## 4. 遗留风险与环境限制（如实说明）

| # | 项目 | 状态 | 建议 |
|---|---|---|---|
| 1 | **本机沙箱对 target/npm 缓存写保护** | 环境限制 | CI（ubuntu）无此问题；本机 spotbugs 用 `-Dspotbugs.skip=true` 或沙箱外执行 |
| 2 | **package-lock.json 未含 eslint/prettier** | 部分完成 | 已改 CI 用 `npm install`；后续网络恢复后可重新 `npm install` 同步 lock |
| 3 | **覆盖率 37%** | 基线值 | docs/25 建议目标（行≥70%/分支≥60%）未达；建议后续按模块补测试（order/marketing/recommend 优先） |
| 4 | SpotBugs 20 项可接受缺陷 | 登记待处理 | 低危；如需清零可逐项人工复核（REC_CATCH 多为设计容错） |
| 5 | Checkstyle 339 warning | 存量格式 | 可逐步用 IDE 自动格式化清理（Indentation/LineLength）；不阻塞构建 |

---

## 5. 修改文件清单

### 新增
- `frontend/eslint.config.js`、`frontend/.prettierrc.json`、`frontend/.prettierignore`
- `backend/checkstyle.xml`、`backend/spotbugs-exclude.xml`

### 修改（代码/配置）
- `frontend/package.json`（devDependencies + lint/format scripts）
- `frontend/src/views/consumer/order/List.vue`（CR-01 契约修复）
- `frontend/src/views/consumer/order/Detail.vue`（CR-02 契约修复）
- `frontend/src/views/admin/product/List.vue`、`src/__tests__/request.spec.js`、`src/__tests__/router.auth.spec.js`、`src/plugins/page-view.js`（CR-10 未使用引用）
- `backend/pom.xml`（checkstyle/spotbugs/jacoco 插件 + verify 绑定）
- `backend/src/main/java/.../security/JwtUtil.java`（CR-04 编码）
- `backend/src/main/java/.../behavior/service/BehaviorService.java`（CR-03 NPE）
- `backend/src/main/java/.../recommend/service/RecommendCalculateService.java`（CR-05/09）
- `backend/src/main/java/.../statistics/service/MerchantStatisticsService.java`（CR-06）
- `backend/src/main/java/.../order/service/RefundService.java`（CR-07 + Logger）
- `backend/src/main/java/.../product/controller/MerchantProductController.java`（CR-07）
- `backend/src/main/java/.../order/component/StockService.java`（CR-08）
- `.github/workflows/ci.yml`（质量门禁 + artifact）

---

## 6. 后续建议（批次 3~5 入口）

1. **批次 3 功能补全决策**：M-01 满赠（实现/裁剪）、LG-04 物流、BE-02 曝光、M-03 限领——需用户拍板
2. **批次 4 动态验证**：E1~E6（端到端冒烟/50 并发/边界样本/退款实测）——需本地 MySQL/Redis
3. **批次 5 文档收口**：docs/09（新接口）、docs/19（M-01 声明）、docs/03（覆盖矩阵）、level1-check/00（状态标注）
4. **覆盖率提升**：按模块补测试至 50%+（优先 order/marketing）
5. **Checkstyle 存量清理**：IDE 自动格式化 Indentation/LineLength
