# 安全自查与审计发现记录

> **日期**：2026-07-07
> **执行人**：Claude Code
> **检查范围**：后端 Java 代码 + 前端 Vue 代码
> **检查项**：SQL 注入、XSS、越权访问、鉴权绕过、敏感信息泄露、幂等性、输入校验

---

## 1. SQL 注入

| 检查项 | 方法 | 结果 |
|--------|------|------|
| 原生 SQL 拼接 | `grep -r "createQuery\|SqlSession\|prepareStatement\|execute("` | ✅ **未发现** — 全部使用 MyBatis-Plus 参数化查询 |
| MyBatis XML 中的 ${} | 检查 mapper XML | ✅ **未发现** — 仅使用 `#{}` 参数化 |
| 动态查询条件 | 检查 LambdaQueryWrapper 使用 | ✅ 安全 — MyBatis-Plus 自动参数化 |

**结论：无 SQL 注入风险。**

---

## 2. XSS（跨站脚本攻击）

| 检查项 | 方法 | 结果 |
|--------|------|------|
| 前端 v-html 使用 | `grep -r "v-html"` | ✅ **未发现** |
| 前端 innerHTML 赋值 | `grep -r "innerHTML"` | ✅ **未发现** |
| 前端 document.write | `grep -r "document.write"` | ✅ **未发现** |
| 前端 eval() | `grep -r "eval("` | ✅ **未发现** |
| 后端富文本存储 | 商品 detail 为 LONGTEXT，前端渲染方式 | ⚠️ **需关注** — 富文本内容由后端存储，前端需使用 v-html 渲染时需做 sanitize |

**结论：当前无 XSS 风险。后续若富文本渲染需引入 DOMPurify。**

---

## 3. 越权访问

| 检查项 | 方法 | 结果 |
|--------|------|------|
| 订单详情归属校验 | `OrderService.getDetail()` | ✅ `order.getUserId().equals(userId)` 校验 |
| 商家订单归属校验 | `OrderService.getMerchantOrderDetail()` | ✅ `order.getShopId().equals(shopId)` 校验 |
| 商家商品归属校验 | 检查 MerchantProductController | ✅ 按 shopId 过滤 |
| 地址归属校验 | 检查 AddressService | ✅ 按 userId 过滤 |
| 评价归属校验 | `ReviewService.submit()` | ✅ 通过 OrderItem 间接校验 |

**结论：关键资源路径均已做归属校验。**

---

## 4. 鉴权绕过

| 检查项 | 方法 | 结果 |
|--------|------|------|
| JWT 白名单路径 | `JwtInterceptor.WHITE_LIST` | ✅ 仅公开资源放行 |
| 可选登录逻辑 | 白名单路径中携带合法 Token 时解析 userId | ✅ 实现正确 |
| 角色注解 @RequireRole | 检查 admin 控制器 | ✅ 全部 `@RequireRole(3)` |
| 商家接口角色约束 | 检查 merchant 控制器 | ✅ `@RequireRole(2)` |
| 消费者接口角色约束 | 检查 consumer 控制器 | ✅ `@RequireRole(1)` |

**结论：鉴权体系完整，无绕过风险。**

---

## 5. 敏感信息泄露

| 检查项 | 方法 | 结果 |
|--------|------|------|
| 密码返回 | 检查所有 VO/DTO | ✅ **未发现** — UserVO/LoginVO 均不含 password |
| JWT Secret 硬编码 | `grep -r "secret"` | ✅ 使用 `${JWT_SECRET}` 环境变量注入 |
| 数据库密码硬编码 | `grep -r "password" application*.yml` | ✅ 使用 `${DB_PASSWORD}` 环境变量 |
| 堆栈信息泄露 | `GlobalExceptionHandler` | ✅ 返回 ErrorCode.message，不返回 stackTrace |
| API 文档暴露 | Knife4j 配置 | ⚠️ `doc.html` 在生产环境应禁用 |

**结论：敏感信息未泄露。建议生产环境禁用 Knife4j 接口文档。**

---

## 6. 幂等性

| 检查项 | 方法 | 结果 |
|--------|------|------|
| 下单幂等 | `OrderService.createOrder()` | ✅ Redis SET NX + 24h TTL |
| 评价幂等 | `ReviewService.submit()` | ✅ 同一 orderItemId 只能评价一次 |
| 支付幂等 | `OrderService.handlePayCallback()` | ✅ 校验订单状态，已支付直接返回 |
| 优惠券领取幂等 | `CouponService.receive()` | ✅ user_coupon 唯一约束 |

**结论：关键写操作均有幂等保护。**

---

## 7. 输入校验

| 检查项 | 方法 | 结果 |
|--------|------|------|
| DTO 校验注解 | `@Validated` + `@NotNull`/`@NotBlank` | ✅ 注册/登录/下单等核心接口均有 |
| 金额计算精度 | BigDecimal 使用 | ✅ 统一 BigDecimal |
| 文件上传大小 | `application.yml` multipart 配置 | ✅ max-file-size=10MB |
| 分页参数上限 | 检查 pageSize | ⚠️ 未发现硬上限（规范要求 ≤100） | ⚠️ 低危 |

---

## 8. 审计发现汇总

### 高危（0 项）

无。

### 中危（1 项）

| # | 发现 | 位置 | 建议 |
|---|------|------|------|
| 1 | Knife4j API 文档在生产环境可能暴露接口结构 | `MallApplication` / `pom.xml` | 生产 profile 排除 knife4j 依赖或关闭文档路径 |

### 低危（2 项）

| # | 发现 | 位置 | 建议 |
|---|------|------|------|
| 1 | 分页 `pageSize` 无硬上限 | 各分页接口 | 添加 `@Max(100)` 校验 |
| 2 | 富文本渲染需 sanitize | 商品详情页 | 引入 DOMPurish 或使用 v-richtext |

### 已修复（本次修补中）

| # | 发现 | 修复 |
|---|------|------|
| 1 | 评价功能后端缺失（review 表有 DDL 但无实现） | 已补全 Review 全链路 |
| 2 | 消费者端促销查询接口缺失 | 已新建 PromotionController |
| 3 | BigDecimal 精度问题（1-percent 使用 double） | 改为 `BigDecimal.ONE.subtract(pct)` |

---

## 9. 合规性

| 检查项 | 结果 |
|--------|------|
| 密码 BCrypt 加密存储 | ✅ |
| JWT 过期时间配置 | ✅ 7 天（`expire-seconds=604800`） |
| 安全响应头（XSS/Content-Type 等） | ✅ `SecurityHeadersFilter` |
| Redis key 前缀规范 | ✅ `RedisKeyPrefix` |
| 逻辑删除全局配置 | ✅ MyBatis-Plus 全局配置 |
| 操作日志记录 | ✅ `OperationLogService` |

**总体安全评级：良（B+）**
- 核心安全措施（SQL 注入防护、XSS 防护、鉴权体系、敏感信息保护）均已到位
- 剩余中/低危项为非阻断性改进建议，不影响上线
