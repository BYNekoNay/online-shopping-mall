# 修复计划（基于三级检查结果）

> **版本**：v1.0
> **日期**：2026-08-13
> **编制**：SeniorDeveloper
> **依据**：docs/21 系统架构梳理报告、docs/22 检查计划、docs/level1-check/00~14 三级检查报告（P1=13、P2=79、P0=0）
> **目标**：在保持既有良好实践（防超卖、防 TOCTOU、防 IDOR、BCrypt/JWT）前提下，按优先级系统性修复全部 P1 与 P2，恢复需求覆盖率达 100%（或文档化裁剪），并补齐动态验证。

---

## 第 1 部分 文档与代码对应关系（整理结果）

### 1.1 文档体系索引（docs/ 共 24 篇 + level1-check 15 篇）

| 类别 | 文档 | 性质 | 与代码的关系 |
|---|---|---|---|
| 需求 | `03-软件需求规格说明书.md` | 需求基线 FR-C/M/A/S/X | 需求覆盖矩阵见 level1-check/14 §1 |
| 设计 | `04-概要设计` / `05-详细设计` | 架构与模块设计 | 与 21 架构报告一致 |
| 契约 | `09-接口规范.md` | **接口字段级契约（权威）** | 与 Controller/DTO 比对；F-01 违反此契约 |
| 数据 | `10-数据库规范.md` | 25 表 DDL 规范 | 与 V1__init_schema.sql 对应 |
| 算法 | `12-核心算法设计文档.md` | UserCF/ItemCF 数学推导 | 与 RecommendCalculateService 逐项核对通过 |
| 发布 | `19-v1.0-发布说明.md` | v1.0 声称功能清单 | 发现 M-01（满赠）声称实现但未实现 |
| 架构 | `21-系统架构梳理报告.md` | 代码现状架构分析 | 本文档的结构基础 |
| 检查 | `22-代码与业务逻辑检查计划.md` | 三级检查方法/分级标准 | 问题分级 P0/P1/P2 定义 |
| 报告 | `level1-check/00~14` | 三级检查问题登记 | **本修复计划的问题来源** |
| CI/CD | `20-CI-CD部署指南.md` | 构建/发布流水线 | 与 .github/workflows 对应 |

### 1.2 代码库结构（后端 11 模块 + 前端 4 域）

```
backend/src/main/java/com/pzhu/mall/
├── modules/                          # 11 个业务模块
│   ├── user/        UserService/AddressService/LoginAttemptService
│   ├── shop/        ShopService（入驻审核闭环）
│   ├── product/     ProductService/CategoryService/ReviewService + MerchantProductController
│   ├── cart/        CartService
│   ├── order/       OrderService(706L)/OrderGroupProcessor(267L)/RefundService(225L)
│   │                component/StockService(206L)/task/OrderTimeoutTask
│   ├── recommend/   RecommendCalculateService(819L)/RecommendService(318L)/RecommendRefreshTask
│   ├── marketing/   CouponService(304L)/PromotionService(185L)/PointsService(211L)
│   ├── logistics/   FreightService/LogisticsQueryService
│   ├── behavior/    BehaviorService
│   ├── statistics/  MerchantStatisticsService/PlatformStatisticsService/StatisticsSnapshotTask
│   └── admin/       Admin*Controller/OperationLogService/SystemConfigService
├── common/          Result/PageResult/ErrorCode/GlobalExceptionHandler
├── config/          Redis/MyBatis-Plus/Redisson 配置
└── security/        JwtInterceptor/RequireRole/RoleCheckAspect/LoginUserContext

frontend/src/
├── api/            12 个 API 模块（product/order/cart/recommend…）
├── router/index.js 三端路由 + 全局守卫（requiresAuth/roles）
├── store/          user/cart/app/merchant（Pinia）
├── views/          consumer(7域)/merchant(5域)/admin(6域)/common
├── layouts/        3 端布局（MerchantLayout 按 shopStatus 重定向）
├── components/     RecommendList.vue
├── utils/request.js axios 封装（错误码 10003→/403 等）
├── plugins/page-view.js 埋点
└── mock/           11 个 Mock 模块

backend/src/test/java/               # 22 个测试类（基线 37+2+10 用例）
docs/level1-check/                   # 15 份检查报告（00 汇总 + 01~14）
```

### 1.3 关键业务链路 ↔ 代码 ↔ 文档对照

| 链路 | 代码落点 | 关联文档 | 已知问题 |
|---|---|---|---|
| 下单（跨店拆单） | OrderService.createOrder → OrderGroupProcessor | 09 §3.2 / 11 §4.1 | O-03/O-08/O-11/O-12、F-01、M-02 |
| 支付 | OrderService.pay → StockService | 09 §3.4 | O-04/O-05/O-15 |
| 取消/超时回退 | OrderService.doCancel / OrderTimeoutTask | 11 §4.3/§4.4 | L2-02（Redis 在事务内） |
| 退款 | RefundService.apply/audit | 09 §3.5 | O-01/O-02、L2-05 |
| 推荐 | BehaviorService → RecommendCalculateService → RecommendService | 12 §9 | L2-01、R-01~R-08、BE-02 |
| 统计 | Merchant/PlatformStatisticsService + SnapshotTask | 03 FR-S-* | ST-01~ST-06 |
| 商家入驻 | ShopService.apply/audit + AdminShopController | 03 FR-M-01 | AD-01、SH-01~03 |
| 库存 | StockService(Redis Lua) + OrderService(DB 实扣) | 11 §4.2 | O-01/O-04、L2-03 |

---

## 第 2 部分 已识别问题清单（P1 全量 + P2 分组）

### 2.1 P1 问题（13 个，含影响范围）

| # | 编号 | 位置 | 问题 | 影响范围 |
|---|---|---|---|---|
| 1 | AD-01 | AdminUserController / UserService.register / ShopService.audit | 用户角色分配接口缺失 + 入驻审核不升级 role=2 | **商家端整体不可用**（/api/merchant/* 与前端 /merchant 全 403） |
| 2 | F-01 | frontend/views/consumer/order/Confirm.vue:179-204 | 券字段双重错误：createOrder 传 couponId 应为 userCouponId；estimate 传记录 ID 应为模板 ID | **优惠券功能整体失效**（不折扣/不核销/估价恒 0） |
| 3 | O-01 | RefundService.java:143-186 | 退款审核通过不恢复库存 | 库存永久丢失 |
| 4 | M-02 | CouponService + OrderGroupProcessor:139-141 | 店铺券/品类券不校验适用范围 | 跨店/跨品类错误折扣（资金相关） |
| 5 | O-05 | OrderService.pay / PaymentMapper | payment 表死表：支付记录未落库 | 任务书 7.3 支付记录需求未落地 |
| 6 | O-02 | RefundService.java:64-68 | 退款幂等键在校验前写入 | 校验失败后 72h 无法重试 |
| 7 | O-04 | OrderService.java:558-564 | product.stock 非原子读改写 | 并发支付商品总库存漂移 |
| 8 | M-01 | PromotionService case3 + OrderGroupProcessor | 满赠促销(type=3)未实现 | 需求未实现 + docs/19 声称失真 |
| 9 | O-03 | OrderService.java:241-243,269-279 | 下单两处 catch 吞异常无日志 | 排障黑洞 |
| 10 | ST-01 | MerchantStatisticsService:36-48,67-73 | 销售统计未过滤订单状态 | 销售额/热销 TOP10 虚高，与 GMV 矛盾 |
| 11 | L2-01 | RecommendCalculateService.calculateForUser | 准实时推荐无调用方（死代码） | 创新点④"准实时"实为日更 |
| 12 | L2-02 | OrderService.doCancel | Redis 库存归还在 DB 事务内 | 事务失败 → 库存虚增 |
| 13 | L2-03 | MerchantProductController.update | 改库存不同步 Redis stock key | Redis/DB 库存永久漂移，可能超卖 |

### 2.2 P2 问题（79 个，按修复主题归组）

| 主题组 | 编号（数量） | 代表问题 |
|---|---|---|
| A. DTO/入参校验（~18） | O-09/O-10/O-14/O-15、P-04/P-11/P-12、U-02/U-03、M-06、SH-01、LG-02、AD-05、R-08、CR-02、O-10 | DTO 缺 @Valid；评分无 1~5；密码长度不一致 |
| B. 异常/日志（~8） | O-03(兼)、P-05、U-01、AD-02/AD-03/AD-06、O-08、SH-02 | 吞异常；审核不检查影响行数；日志覆盖不全 |
| C. 幂等/并发边界（~6） | O-08、CR-03、M-03、U-05/U-06、P-08 | 下单幂等键前置；购物车并发超限；限领规则 |
| D. 统计口径（~6） | ST-02~ST-06、BE-02 | 今日订单口径；CTR 近似；漏斗单位；预计算空实现 |
| E. 性能（~9） | M-05、AD-04、R-03/R-04、ST-06、O-07、O-13、F-04、P-06 | N+1；全量重算；无分页；全量内存聚合 |
| F. 死代码/未实现（~8） | R-01/R-02、ST-05、M-01(兼)、LG-04、M-08、O-06、P-07 | 死分支；模拟物流；死状态；scope 仅 SHOP |
| G. 契约/前端（~6） | F-02~F-07、L2-06、O-12、P-02/P-09 | 匿名停留缺失；收藏重复；分页未接；券语义 |
| H. 数据一致性（~5） | P-06、LG-01/LG-03、CR-04、L2-04/L2-05、L3-01 | stock 口径；模板 LIMIT 1；track 越权；上传无 volume |

---

## 第 3 部分 修复任务（按优先级排序）

> 排序逻辑：**主链路可用性 → 资金/库存正确性 → 契约一致性 → 口径统一 → 性能/规范**。
> 每项均标注：目标 / 涉及文件 / 预期结果 / 验证。

### 阶段 1：商家角色链路打通（P1：AD-01 + F-05 联动）

| 项 | 内容 |
|---|---|
| **目标** | 商家入驻审核通过后自动获得商家角色（role=2），商家端完整可用 |
| **涉及文件** | ① `AdminUserController.java`（新增角色分配接口）② `ShopService.java`（audit 通过时事务内升级 role）③ 前端 `Login.vue` / `router/index.js`（登录跳转逻辑）④ `AdminUser` 相关 VO/DTO |
| **实现要点** | 推荐方案：`ShopService.audit(approve)` 通过分支内 `UserService.updateRole(userId, 2)`，与 shop.status 更新同事务；同时保留管理员手动分配接口 `PUT /api/admin/users/{id}/role`（任务书 FR-A-01） |
| **预期结果** | 审核通过商家刷新后访问 /merchant/* 不再 403；管理员可在用户管理页改角色 |
| **验证** | ① 单测：审核通过 → role 断言 2 ② 集成：商家注册→入驻→审核→登录商家端全链路 ③ 回归 RoleCheckAspect 既有测试 |
| **时长** | 3~4h |

### 阶段 2：优惠券链路修复（P1：F-01 + M-02 + O-12 联动）

| 项 | 内容 |
|---|---|
| **目标** | 优惠券从"领券→估价→下单→核销"全链路正确，券适用范围受控 |
| **涉及文件** | ① `frontend/views/consumer/order/Confirm.vue`（字段修正）② `CouponService.java`（calculateDiscountByUserCoupon 前置校验归属/状态/有效期 + 店铺/品类匹配）③ `OrderGroupProcessor.java:139-141`（券适用范围）④ `OrderEstimateController.java`（O-12 语义统一） |
| **实现要点** | F-01：createOrder 传 `userCouponId`（UserCoupon 记录 ID）；estimate 传 `couponId`（模板 ID）。M-02：校验①UserCoupon 归属+status=0+有效期 ②店铺券匹配分组 shopId ③品类券匹配分组内商品 categoryId，不匹配返回 0 折扣 |
| **预期结果** | 下单券折扣生效并核销；估价与实际一致；跨店/跨品类券无效 |
| **验证** | ① 前端契约测试（下单/估价参数）② 单测：店铺券跨店 → 0 折扣；品类券跨品类 → 0 折扣 ③ 集成：领券→估价→下单→券核销→取消归还全链路 |
| **时长** | 4~5h |

### 阶段 3：退款链路修复（P1：O-01 + O-02；P2：L2-05 规则确认）

| 项 | 内容 |
|---|---|
| **目标** | 退款审核通过后库存恢复、幂等键可重试、退款券规则明确 |
| **涉及文件** | ① `RefundService.java`（audit 恢复库存：按 OrderItem 归还 sku/product，与 doCancel 同构；apply 幂等键后置）② `StockService.java`（复用 rollback/rollbackProduct）③ 与 docs/09 同步 |
| **实现要点** | O-01：audit 通过后在事务内按 order_item（含 skuId/productId/qty）归还库存（DB 原子 UPDATE stock+qty）+ Redis INCR。O-02：SET NX 移至全部校验通过之后写入；失败分支删除键 |
| **预期结果** | 退款后商品库存恢复；校验失败可立即重试；退款不影响 Redis/DB 一致性 |
| **验证** | ① 单测：RefundServiceTest 扩展（audit 恢复库存断言）② 集成：下单→支付→退款→库存核对 ③ 幂等重试场景实测 |
| **时长** | 4~5h |

### 阶段 4：库存一致性修复（P1：O-04 + L2-03）

| 项 | 内容 |
|---|---|
| **目标** | product.stock 原子化；管理员/商家改库存同步 Redis |
| **涉及文件** | ① `OrderService.java:558-564`（O-04）② `MerchantProductController.java`（L2-03，create/update 后同步 Redis key）③ `StockService.java`（提供 refresh 方法） |
| **实现要点** | O-04 方案A：改为主键原子 `UPDATE product SET stock=GREATEST(stock-qty,0)`；方案B：SKU 商品展示侧聚合（需评估前端影响）。L2-03：改库存成功后 del Redis key（触发懒加载）或 set 新值 |
| **预期结果** | 并发支付不同 SKU 商品总库存不漂移；改库存后 Redis/DB 一致 |
| **验证** | ① 单测：并发扣减断言 ② 集成：改库存→下单→Redis key 值核对 ③ 50 并发下单压测（E6） |
| **时长** | 4h |

### 阶段 5：订单健壮性收尾（P1：O-03 + O-05）

| 项 | 内容 |
|---|---|
| **目标** | 下单异常可追踪；支付记录落库 |
| **涉及文件** | ① `OrderService.java:241-243,269-279`（catch 补 log.error）② `OrderService.pay` + `PaymentMapper.java` + `Payment` 实体（O-05）③ 前端支付页（如需展示支付记录） |
| **实现要点** | O-05 需先与 docs/09 核对：若定义支付记录则 pay 事务内插入 Payment（orderId/payType/amount/status）；若属"模拟支付裁剪"则更新 docs/09 + 19 声明（推荐裁剪声明，毕设模拟支付场景） |
| **预期结果** | 异常根因可查；支付记录需求有明确落点（实现或裁剪声明） |
| **验证** | ① 单测：pay 后 Payment 断言（若实现）② 集成：支付链路回归 ③ grep 确认无裸 catch |
| **时长** | 2~3h |

### 阶段 6：统计口径统一（P1：ST-01；P2：ST-02~06 随批）

| 项 | 内容 |
|---|---|
| **目标** | 商家销售统计与平台 GMV 同口径，统计可信 |
| **涉及文件** | ① `MerchantStatisticsService.java:36-48,67-73`（SQL 加 status IN (1,2,3,4,6)）② `PlatformStatisticsService.java`（ST-02 今日订单过滤）③ `StatisticsSnapshotTask.java`（ST-05 空实现/死缓存清理） |
| **实现要点** | 销售总额/订单数/热销 TOP10 统一过滤已支付状态；快照缓存接入读路径或删除；CTR 口径依赖 BE-02 决策（见阶段 9） |
| **预期结果** | 商家统计与平台看板一致；无死缓存 |
| **验证** | ① 单测：含待付款/取消/退款订单的统计断言 ② 集成：造数据比对商家/平台统计 |
| **时长** | 2~3h |

### 阶段 7：推荐准实时接线（P1：L2-01；P2：R-05 随批）

| 项 | 内容 |
|---|---|
| **目标** | 行为触发后准实时刷新推荐，创新点④名副其实 |
| **涉及文件** | ① `BehaviorService.java` 或 `OrderService.pay`（购买后异步触发）② `RecommendCalculateService.java`（calculateForUser 复用/改造）③ `RecommendService.java`（R-05 缓存清理：定时任务批量删除用户 ZSet） |
| **实现要点** | 方案A（推荐）：行为记录/支付后异步提交 calculateForUser（低频合并：同一用户 5 分钟内合并一次）；方案B（保守）：定时增量刷新（每 10 分钟刷新增量行为用户）。注意 calculateForUser 当前全量重算（R-03），需先预热相似度矩阵 |
| **预期结果** | 收藏/购买后 5 分钟内推荐列表反映新行为；缓存不超 24h 陈旧 |
| **验证** | ① 单测：行为触发 → calculateForUser 调用断言 ② 集成：埋点→观察推荐更新延迟 ③ 回归 RecommendCalculateServiceTest |
| **时长** | 4~5h（含 R-03/R-04 预热） |

### 阶段 8：满赠促销决策与实现（P1：M-01）—— 需需求确认

| 项 | 内容 |
|---|---|
| **目标** | 满赠(type=3)要么实现、要么文档化裁剪（消除 docs/19 失真） |
| **涉及文件** | ① `PromotionService.java`（calculateSingleDiscount case3）② `OrderGroupProcessor.java`（赠品行插入 is_gift=1 + 预扣库存）③ `19-v1.0-发布说明.md` / `docs/10`（同步声明） |
| **实现要点** | 实现：订单明细达标后按 rule 插入赠品行（单价 0、is_gift=1、预扣库存）；裁剪：19/10 文档声明"满赠为后续扩展，v1.0 不支持"。**建议答辩完整度优先 → 实现** |
| **预期结果** | 满赠活动正常出赠品；或文档如实声明 |
| **验证** | ① 单测：PromotionServiceTest 满赠用例 ② 集成：满赠下单→赠品行断言 ③ 需求覆盖矩阵更新 |
| **时长** | 实现 5~6h / 裁剪 1h（**需用户拍板**） |

### 阶段 9：P2 批量修复（79 个按主题组执行，每日一组）

| 组 | 内容 | 涉及文件 | 时长 |
|---|---|---|---|
| A. DTO 校验 | O-09/10/14/15、P-04/11/12、U-02/03、M-06、SH-01、LG-02、AD-05、R-08、CR-02 | 各模块 Controller/DTO | 4~5h |
| B. 异常/日志 | P-05、U-01、AD-02/03/06、SH-02、O-08 | MerchantProductController/UserService/Admin*Controller | 3~4h |
| C. 幂等/并发 | CR-03、M-03（需确认）、U-05/06、P-08、O-13 | CartService/CouponService/AddressService/ProductService/OrderTimeoutTask | 3~4h |
| D. 统计/曝光 | ST-02~06、BE-02（曝光落库决策） | PlatformStatisticsService/StatisticsSnapshotTask/BehaviorService | 3~4h |
| E. 性能 | M-05、AD-04、R-03/04、ST-06、O-07、F-04 | CouponController/AdminProductController/Recommend*/OrderService | 3~4h |
| F. 死代码/契约 | R-01/02、ST-05、LG-04（裁剪声明）、M-08、O-06、P-07、O-12 | Recommend*/Statistics/Logistics/Promotion/OrderEstimate | 3~4h |
| G. 前端 | F-02~07、L2-06、P-02 前端提示 | page-view.js/behavior.js/Favorites.vue/Refunds.vue | 3~4h |
| H. 数据/安全 | P-06、LG-01/03、CR-04、L2-04（track 越权）、L3-01（volume） | MerchantProductController/FreightService/CartService/LogisticsQueryService/docker-compose.yml | 3~4h |

---

## 第 4 部分 验证与测试策略

### 4.1 回归基线（每次修复后必须执行）

```
mvn test                     # 既有 22 个测试类（37+2+10 用例基线）
npm run test:unit            # 前端单测（路由守卫/请求拦截 + 新增契约测试）
```

### 4.2 新增测试计划（随修复同步补）

| 修复 | 新增测试 |
|---|---|
| AD-01 | AdminUserController 角色分配单测；ShopService.audit 角色联动单测 |
| F-01/M-02 | Confirm.vue 契约测试（userCouponId/couponId 断言）；CouponService 跨店/跨品类用例 |
| O-01/O-02 | RefundServiceTest 库存恢复/幂等重试用例 |
| O-04/L2-03 | 并发扣减单测；Redis key 同步断言 |
| ST-01 | 统计状态过滤用例 |
| L2-01 | 行为触发 calculateForUser 断言 |
| M-01 | PromotionService 满赠用例（若实现） |
| L2-04 | track 归属校验用例 |

### 4.3 动态验证（需本地 MySQL/Redis，全部修复完成后执行）

1. **端到端冒烟**：消费者注册→浏览→加购→下单→支付→收货→评价→推荐；商家入驻→审核→发布→发货→统计；管理员审核→看板
2. **并发压测**：50 并发下单同一 SKU（验证无超卖 + L2-03 修复后 Redis/DB 一致）
3. **边界样本**：level1-check/14 §4 E1~E10 逐条 curl
4. **退款实测**：验证 O-01 库存恢复 + O-02 幂等重试
5. **推荐准实时**：行为触发后观察推荐更新延迟（L2-01）
6. **安全回归**：track 越权修复验证（L2-04）

### 4.4 文档同步清单（修复收尾）

- [ ] `docs/09-接口规范.md`：新增/变更接口（角色分配、支付记录若实现）
- [ ] `docs/19-v1.0-发布说明.md`：修正 M-01 满赠声明；补角色联动说明
- [ ] `docs/03-软件需求规格说明书.md`：需求覆盖矩阵更新为 100%/裁剪标注
- [ ] `docs/level1-check/00-汇总.md`：问题状态改为"已修复/已裁剪"

---

## 第 5 部分 时间预估与风险评估

### 5.1 时间预估（单人开发，含测试与回归）

| 阶段 | 内容 | 预估时长 | 累计 |
|---|---|---|---|
| 1 | 商家角色链路 | 3~4h | 3~4h |
| 2 | 优惠券链路 | 4~5h | 7~9h |
| 3 | 退款链路 | 4~5h | 11~14h |
| 4 | 库存一致性 | 4h | 15~18h |
| 5 | 订单健壮性 | 2~3h | 17~21h |
| 6 | 统计口径 | 2~3h | 19~24h |
| 7 | 推荐准实时 | 4~5h | 23~29h |
| 8 | 满赠（实现/裁剪） | 1~6h | 24~35h |
| 9 | P2 批量（8 组） | 25~30h | 49~65h |
| 10 | 动态验证 + 文档同步 | 8~12h | **57~77h（约 8~10 个工作日）** |

> ⚠️ 若用户可拍板裁剪 M-01/LG-04/O-05（文档化声明），总时长可压缩约 10h。

### 5.2 风险评估

| 风险 | 等级 | 缓解措施 |
|---|---|---|
| 退款库存恢复引入资金/库存双计错误 | 🔴 高 | 与 doCancel 复用同构逻辑；单测+实测双重验证；先小流量 |
| 优惠券链路改动破坏计价 | 🔴 高 | F-01 先加前端契约测试再改；M-02 校验失败返回 0 折扣不抛错（防御式） |
| 库存 Redis/DB 同步遗漏路径 | 🟠 中 | grep 全仓库 updateById(stock) 路径清单化；改后统一走 StockService.refresh |
| L2-01 准实时接线导致推荐计算压力 | 🟠 中 | 异步 + 同用户合并节流；先预热相似度矩阵（R-03） |
| 动态验证环境搭建耗时（本地 MySQL/Redis 配置） | 🟡 低 | 复用 docker-compose.yml 一键启动；dev profile |
| P2 批量改动面大引入回归 | 🟡 低 | 每日一组 + 每次跑全量测试基线；按主题提交便于回滚 |
| 需求裁剪决策（M-01/O-05/LG-04）影响答辩完整性 | 🟠 中 | 提前与导师确认；裁剪均文档化声明 + 标注"后续扩展" |

### 5.3 建议执行节奏

- **Day 1-2**：阶段 1~3（主链路 + 资金/库存）
- **Day 3-4**：阶段 4~7（一致性/统计/推荐）
- **Day 5**：阶段 8（满赠决策与实现/裁剪）
- **Day 6-9**：阶段 9（P2 每日一组）
- **Day 10**：动态验证 + 文档同步 + 发布 v1.1

---

## 附录：问题清单速查（编号→报告）

| 编号来源 | 报告 |
|---|---|
| O-01~O-15 | level1-check/01-order.md |
| P-01~P-12 | level1-check/02-product.md |
| U-01~U-07 | level1-check/03-user.md |
| R-01~R-08 | level1-check/04-recommend.md |
| M-01~M-09 | level1-check/05-marketing.md |
| CR-01~CR-04 | level1-check/06-cart.md |
| SH-01~SH-03 | level1-check/07-shop.md |
| BE-01~BE-03 | level1-check/08-behavior.md |
| LG-01~LG-04 | level1-check/09-logistics.md |
| ST-01~ST-06 | level1-check/10-statistics.md |
| AD-01~AD-06 | level1-check/11-admin.md |
| F-01~F-07 | level1-check/12-frontend.md |
| L2-01~L2-07 | level1-check/13-level2-模块间联系.md |
| L3-01~L3-03 | level1-check/14-level3-整体系统.md |
