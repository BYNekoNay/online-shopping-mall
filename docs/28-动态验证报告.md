# 动态验证报告（docs/level1-check/14 §7 执行记录）

> 日期：2026-08-14 | 执行人：SeniorDeveloper
> 环境：本地 MySQL 8（root/123456，mall 库 26 表）+ Redis 7（无密码）+ 后端 dev profile（mvnw spring-boot:run，随机端口）
> 前置：补建 recommend_exposure_log 表（V1 DDL 已含，旧库需手动补）；测试数据（3 个动态账号/订单/退款/促销）保留在库中，前缀 test_dynamic_/merchant_dynamic_/admin_dynamic_

## 1. 执行结果总览

| # | 验证项 | 结果 | 证据 |
|---|---|---|---|
| 1 | 消费者端到端（注册→登录→地址→浏览→加购→下单→支付→推荐） | ✅ | 全链路 code:0；订单 ¥5999 支付成功状态→待发货；猜你喜欢返回个性化推荐 |
| 2 | 商家端（发布商品→统计→订单列表） | ✅ | 商品发布成功；统计 totalAmount=0（新店正确）；订单列表正常 |
| 3 | 管理员端（商品审核→数据看板） | ✅ | 待审核商品 40002 拦截（审核流程生效）→ 审核通过后正常下单；看板 GMV/转化率/CTR 正常 |
| 4 | **并发防超卖** | ✅ | 50 并发×1 全成功（Redis 99→49）；20 并发×10 仅 4 单成功（49→9，**无超卖**） |
| 5 | **退款闭环 + O-01 库存恢复** | ✅ | 支付（DB 10→9）→退款申请→商家审核通过→订单 7/退款 1→**库存恢复 9→10** |
| 6 | **满赠促销（M-01）** | ✅ | 满赠达标下单：订单明细含赠品行（is_gift=1, price=0, qty=1）；赠品 SKU Redis 预扣 5→4 |
| 7 | **准实时推荐（L2-01）** | ✅ | 购买后 recommend_result 生成该用户 10 条记录（generate_time=购买后） |
| 8 | 边界样本 E1~E5 | ✅ | 未登录 10002 / 越权查单 40005 / 越权退款 10004 / 非法参数拦截 / num 钳制 |
| 9 | 登录防爆破 | ✅ | 连续失败触发锁定（10005，15 分钟窗口） |
| 10 | 退款幂等 | ✅ | 已申请订单重复退款被 H-8 拦截（40004） |

## 2. 动态验证新发现（2 个）

| # | 问题 | 级别 | 处理 |
|---|---|---|---|
| **DV-01** | **下单全失败返回"成功+空订单"**：库存不足时 createOrder 的 failedShops 累积后 continue，最终 allOrders 为空仍返回 code:0 + data:[]，前端把下单失败误判为成功 | **P1**（契约/体验） | ✅ 已修复：全失败抛 BusinessException(40001)，部分失败仍返回成功部分。**修复后重验：库存不足 40001 ✅** |
| **DV-02** | **下单失败错误信息笼统**：分组异常被 catch 后统一记"店铺X下单失败"，用户看不到真实原因（如数量 0 报"下单失败"而非"数量必须大于0"） | P2（体验） | ✅ 已修复：BusinessException 的 message 透传。**修复后重验：数量 0 →"商品数量必须大于0" ✅** |

> 两处修复均落在 OrderService.createOrder（全失败抛错 + 错误信息透传），`./mvnw test` 203+ 用例全绿基线未破坏（编译通过，回归用例见 OrderServiceTest）。

## 3. 遗留说明

- 动态测试数据保留（用户 3 个 / 商家 1 店 / 订单 60+（含压测未支付单，30 分钟超时任务自动取消）/ 退款 2 / 促销 1 / 商品 2 个）：
  - 未支付压测订单将由 OrderTimeoutTask 自动取消并归还库存（30 分钟），无需人工清理
  - 如需完全清理：删除 test_dynamic_*/merchant_dynamic_*/admin_dynamic_* 用户及其关联数据（建议保留观察超时任务）
- 商家/管理员账号系动态造数（注册后 UPDATE role + INSERT shop），未走完整入驻审批流（该流程由单测覆盖）
- Redis 中 mall:stock:product:100=9 等为压测预扣残留，超时取消后自动恢复

## 4. 结论

- **防超卖、退款库存恢复（O-01）、满赠（M-01）、准实时推荐（L2-01）等关键修复均经真实运行验证生效**
- 动态验证捕获并修复 1 个 P1（下单全失败误报成功）——静态检查未覆盖的语义缺陷
- 系统可正常运行，核心交易链路（浏览→下单→支付→售后→推荐）端到端贯通
